To run correctly the program you have to pass the input as argument.

Example:

./lab_2.exe <directory_path> <test_image_path>

- directory_path:	it's the directory containing all the images framing a checkerboard for calibration purpose
- test_image_path:	distorted image